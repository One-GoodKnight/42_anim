accents lower case
shuffle letters inside current words only, not outside it
better contrl + (del | supp)
remove spaces at the end when checking the end

wait between games
if the host leaves the interface, keep the game running in background
max game duration

make the train take the same amount of time to travel on different screen resolutions
make better random pos for small screen resolutions
